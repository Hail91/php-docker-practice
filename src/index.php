<?php
// ** Data Types **
// 1.) Scalar - Single element field
    // 1.) Boolean (true/false)
    // 2.) Integer (signed)
    // 3.) Float/Double (floating pointer number, ex. 1.4)
    // 4.) String
// 2.) Compound = Elements which contain multiple parts
    // 1.) Array ([1, 2, 3, 4], etc.)
    // 2.) Object 
    // 3.) Callable (ref to a function)
    // 4.) iterable (traversable data structure)
// 3.) Special
    // 1.) resource (a ref to an external resource)
    // 2.) Null (variable with no value, ref that points to nothing)

// ** Standard PHP statements **
?>